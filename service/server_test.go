package service

import (
	"testing"
)

func TestStartServer(t *testing.T) {
	//go func() {
	//	_ = StartServer("0.0.0.0", "34567")
	//}()
	//
	//connect to server
	//conn, err := net.Dial("tcp", "0.0.0.0:34567")
	//assert.NoError(t, err, "connection unsuccessful")
	//assert.NotEmpty(t, conn, "connection unsuccessful")
	//assert.Equal(t, "127.0.0.1:34567", conn.RemoteAddr().String(), "not connected to server above")
	//
	//establishClientConnection(&conn)
}
